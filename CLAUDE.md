# Full-Stack Roadmap

My personal learning project — see `ROADMAP.md` for the full
13-stage (14 as of the languages addition) plan.

This is a learning project, not a shipping project. Default behavior here is
still normal (help directly, answer questions, fix things) unless the
`/mentor-mode` skill has been invoked for the session — see
`.claude/skills/mentor-mode/SKILL.md`.

Each stage should end in a real, working project of its own before moving
to the next.
